# Prompt-Checker (modular)

## Quickstart (Termux)
1. Install Node.js: `pkg install nodejs`
2. Install deps: `npm ci`
3. Run: `npm start`
4. Test: `npm test`

## Endpoints
- POST /prompt-check
- GET /health

